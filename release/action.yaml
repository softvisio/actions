name: Release Action
description: Release action

runs:
  using: composite

  steps:
    - uses: softvisio/actions/checkout@v1

    - uses: softvisio/actions/setup-node@v1

    - shell: bash
      run: |
        npm install -g @softvisio/cli
        softvisio-cli package publish --yes
